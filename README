# zerorpc cross languages tests

Few tests for testing zerorpc across multiple supported languages.
The goal is to catch very visible protocol errors quickly.

## Basics

```shell
./crosstests.py setup
./crosstests.py test
```
